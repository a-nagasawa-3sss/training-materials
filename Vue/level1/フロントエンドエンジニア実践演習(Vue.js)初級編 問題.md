全26問構成となっています。各Stepの流れに沿って、順に問題を進めて下さい。

---

## 1. プロジェクト初期セットアップ
**質問:** 基本的な開発環境とファイル構成を実装してください
**詳細:**  
1. **プロジェクトフォルダの作成:**  
   - 新規フォルダ(例：`todo-app`)を作成する
   - 将来的にTypeScriptへの移行、CSS設計のリファクタリングなどを考慮し、ファイル・ディレクトリの整理がしやすい名前にする
2. **Vueプロジェクトの作成:**  
   - 例として[Vite](https://ja.vitejs.dev/)を利用する場合、以下を実行し、Vue 3プロジェクトを作成する
     ```
     npm create vite@latest todo-app --template vue
     ```     
   - [Vue CLI](https://cli.vuejs.org/)を利用する場合は以下を実行し、Vue 3プロジェクトを作成する
     ```
     vue create todo-app
     ```     
   - プロジェクト作成時に、必要に応じてESLint、TypeScript等のオプションを選択する
3. **基本ファイルの確認:**  
   - 自動生成された`App.vue`, `main.js`(または`main.ts`), `index.html`, `package.json`などのファイル構成を確認する
4. **不要なファイルの削除・整理:**  
   - 自動生成されたデモ用コンポーネントや不要なコメントを削除し、プロジェクトを整理する  
5. **動作確認:**  
   - `npm run dev`コマンドを実行し、開発サーバーが正常に起動するか確認する
   - ブラウザで`http://localhost:3000`(またはCLIの表示に従う)にアクセスして起動を確認する
   
**目的:**  
- Vue 3の基本的な開発環境を構築し、今後の拡張の基盤を作る

**注意点:**  
- Node.jsやnpmのバージョンに注意する
- 必要な依存関係が正しくインストールされているか確認する

**ヒント:**  
- Vue 3の公式ドキュメントを参照する
- Vite、Vue CLIいずれかの手順に合わせて進める

---

## 2. HTML/CSSによる基本UIの構築
**質問:** 基本的なUIレイアウト(ヘッダー、タスク追加セクション、タスクリスト)を実装してください
**詳細:**  
1. **ヘッダーの実装:**  
   - `App.vue`の`<template>`内に`<header>`タグを用いて、画面上部にアプリのタイトル「ToDoリストアプリ」を表示する  
   - 背景色、フォントサイズ、パディングなどのスタイルを設定し、目立つデザインにする
2. **タスク追加セクションの構築:**  
   - `App.vue`にタスクを入力するためのテキスト入力欄(例：`<input type="text" v-model="newTask" placeholder="タスクを入力してください" maxlength="50" />`)を設置する
   - 「追加」ボタンを配置する(例：`<button @click="addTask">追加</button>`)
   - 適切なクラス名(例：`.task-add`)を設定する
3. **タスクリスト表示エリアの構築:**  
   - `<ul>`タグを使用してタスク一覧表示エリアを作成する(例：`<ul class="task-list"></ul>`)
4. **基本スタイルの設定:**  
   - プロジェクトの中で`src/assets/css/main.css`を作成し、各セクションの背景色、フォント、パディング、マージンなど基本スタイルを記述する
   - ページ全体の中央寄せ、最大幅、背景色やフォントの統一を行う
   - または`<style>`ブロックを`App.vue`内に設置して記述してもよい
5. **コメントの記載:**  
   - 各セクションにコメントを追加し、後の機能拡張の際に修正箇所が明確になるようにする

**目的:**  
- 直感的に操作できるシンプルなUIを構築する
- HTMLとCSSの基本的な記述方法およびセマンティックなタグ使用法を理解する

**注意点:**  
- 適切なセマンティックタグと一貫性のある命名規則を使用する

**ヒント:**  
- ワイヤーフレームやMDN Web DocsのHTML/CSSチュートリアルを参照する

---
## 3. JavaScriptでのタスク追加機能の実装
**質問:** タスク追加処理を実装してください
**詳細:**  
1. **データ定義:**  
   - `App.vue`の`<script setup>`(または通常の`<script>`)内で、`ref`を用いて`newTask`(入力値)と`tasks`(タスクリスト)を定義する
2. **タスク追加メソッドの実装:**  
   - `addTask`メソッドを実装し、入力値が空でない場合に`tasks`に新しいタスクオブジェクトを追加する
   - タスクオブジェクトは、`id`, `text`, `completed`のプロパティを持つ  
   - 入力値をクリアし、必要に応じて再びフォーカスを設定する(任意)
3. **タスクリストの表示:**  
   - `v-for`を用いて`tasks`をループし、各タスクを`<li>`要素として表示する
   - タスクのテキストと完了状態を表示する

**目的:**  
- Vue のリアクティブなデータバインディングとイベントハンドリングの基本を学ぶ

**注意点:**  
- `v-for`を使用する際は、一意な`key`属性を設定する

**ヒント:**  
- Vue の公式ドキュメントの「リストレンダリング」を参照する

---
## 4. タスクの削除機能の実装
**質問:** タスク削除の処理を実装してください
**詳細:**  
1. **削除ボタンの追加:**  
   - 各タスクの`<li>`要素内に削除ボタン(例：`<button @click="deleteTask(task.id)">削除</button>`)を追加する
   - ボタンに明確なクラス名やデータ属性を設定し、CSSで削除ボタンのスタイル(背景色、フォント、ホバー効果など)を定義する
2. **削除メソッドの実装:**  
   - `deleteTask`メソッドを実装し、`tasks`から指定された`id`のタスクを削除する

**目的:**  
- Vue でのイベントハンドリングとリストからの要素削除を学ぶ

**注意点:**  
- 削除対象のタスクを正確に特定する

**ヒント:**  
- `Array.prototype.filter()`の使用例を確認する

---
## 5. タスクの完了/未完了切替機能の実装
**質問:** タスクの完了状態切替処理を実装してください
**詳細:**  
1. **完了切替ボタンの追加:**  
   - 各タスクの`<li>`要素内に完了切替ボタン以下を追加する
```html
     <button @click="toggleTask(task.id)">
       {{ task.completed ? '未完了' : '完了' }}
     </button>
```  
   - ボタンのラベルをタスクの完了状態に応じて動的に変更する
2. **完了切替メソッドの実装:**  
   - `toggleTask`メソッドを実装し、指定された`id`のタスクの`completed`プロパティを反転させる
3. **UIの更新:**  
   - タスクの完了状態に応じて、タスクのテキストに打ち消し線を適用するなどのスタイルを変更する

**目的:**  
- Vue での双方向データバインディングと条件付きレンダリングを学ぶ

**注意点:**  
- タスクの完了状態が正しく切り替わることを確認する

**ヒント:**  
- Vue の公式ドキュメントの「クラスとスタイルのバインディング」を参照する

---
## 6. 入力値のバリデーション(空白チェック)の実装
**質問:** 入力値検証(空白チェック)の処理を実装してください
**詳細:**  
1. **バリデーションメソッドの実装:**  
   - `validateInput`メソッドを実装し、入力値が空または空白のみの場合にエラーメッセージを表示する
   - エラーメッセージはリアクティブなデータとして管理し、条件付きレンダリングで表示する
2. **タスク追加メソッドの修正:**  
   - `addTask`メソッド内で`validateInput`メソッドを呼び出し、エラーがある場合はタスク追加処理を中止する

**目的:**  
- Vue での入力バリデーションとエラーハンドリングを学ぶ

**注意点:**  
- エラーメッセージが適切に表示およびクリアされることを確認する

**ヒント:**  
- Vueの公式ドキュメントの「フォーム入力バインディング」を参照する

---
## 7. 重複タスクの検知機能の実装
**質問:** 重複タスク検知の処理を実装してください
**詳細:**  
1. **重複チェックメソッドの実装:**  
   - `isDuplicate`メソッドを実装し、入力値が既存のタスクと重複する場合に`true`を返す
   - タスクのテキストを比較する際に、大文字小文字の違いを無視するなどの前処理を行う(例：`toLowerCase()`)
2. **タスク追加メソッドの修正:**  
   - `addTask`メソッド内で`isDuplicate`メソッドを呼び出し、重複がある場合はエラーメッセージを表示し、タスク追加処理を中止する

**目的:**  
- Vueでのリストデータの検索と重複チェックを学ぶ

**注意点:**  
- 重複チェックの条件を明確にする

**ヒント:**  
- `Array.prototype.some()`の使用例を確認する

---
## 8. タスク情報フィードバック表示機能の実装
**質問:** タスク情報(総タスク数、未完了タスク数)のフィードバック処理を実装してください
**詳細:**  
1. **タスク情報表示エリアの作成:**  
   - `App.vue`(または必要に応じて別コンポーネント)に、統計情報表示用の要素を以下のように作成する
     ```html
     <div>
       総タスク数: {{ totalTasks }}, 
       未完了タスク数: {{ incompleteTasks }}
     </div>
     ```  

2. **算出プロパティの実装:**  
   - `<script setup>`または通常のオブジェクト形式の`computed`プロパティを用いて、総タスク数 (`totalTasks`)と未完了タスク数(`incompleteTasks`)を算出する
   - `totalTasks`は`tasks.length`で求め、`incompleteTasks`は`tasks.filter(task => !task.completed).length`で求める
3. **UIの更新:**  
   - 算出プロパティの値をUIに表示し、タスクの追加、削除、完了状態の変更に応じてリアルタイムに更新されることを確認する
   
**目的:**  
- Vueでの算出プロパティの使用方法と、リアクティブなデータ表示を学ぶ

**注意点:**  
- 算出プロパティが依存するデータ(`tasks`)が変更された際に、正しく再計算されることを確認する

**ヒント:**  
- Vueの公式ドキュメントの「算出プロパティとウォッチャ」を参照する

---
## 9. ローカルストレージによるデータ永続化
**質問:** ローカルストレージへのデータ保存と復元処理を実装してください
**詳細:**  
1. **データ保存処理の実装:**  
   - `watch(tasks, ...)`を用いて`tasks`の変更を監視し、変更時に以下のコードを用いてデータを保存する(Composition API であれば、`import { watch } from 'vue'`を使用)
     ```js
     localStorage.setItem('todos', JSON.stringify(tasks.value))
     ```  
     
2. **データ復元処理の実装:**  
   - `onMounted`を用いてコンポーネントのマウント時に以下のコードでデータを取得・復元し、`tasks`に設定する
     ```js
     const savedData = localStorage.getItem('todos')
     if (savedData) {
       tasks.value = JSON.parse(savedData)
     }
     ```  
    

**目的:**  
- Vueでのウォッチャとライフサイクルフックの使用方法、およびローカルストレージAPIを学ぶ

**注意点:**  
- 保存前後のデータ形式が一致していることを確認する
- ローカルストレージに保存されたデータが正しい形式であることを確認する

**ヒント:**  
- MDNのlocalStorageの使用例を参照する

---
## 10. レスポンシブデザインの適用
**質問:** 異なる画面サイズに対応するレスポンシブデザインを実装してください
**詳細:**  
1. **メディアクエリの追加:**
   - `src/assets/css/main.css`や各コンポーネントの`<style>`内にメディアクエリを記述し、スマートフォン・タブレット・PC向けのスタイルを定義する
   - 例:
     ```css
     @media (max-width: 600px) {
       /* スマホ用スタイル */
     }

     @media (min-width: 601px) and (max-width: 1024px) {
       /* タブレット用スタイル */
     }

     @media (min-width: 1025px) {
       /* PC用スタイル */
     }
     ```
2. **レイアウト調整の実施:**
   - 各セクションの幅、フォントサイズ、パディング、マージンなどを画面サイズに合わせて調整する
   - 例: スマホでは1カラム表示、タブレット以上で2カラム表示に切り替えるなど
3. **動作確認と微調整:**
   - 実機やエミュレーター、ブラウザの開発者ツールのデバイスモードを使い、それぞれの画面サイズで表示が崩れないかテストする

**目的:**  
- レスポンシブデザインの基本を学び、ユーザー体験を向上させる

**注意点:**  
- 各デバイスでの表示確認を十分に行う

**ヒント:**  
- ブラウザの開発者ツール(デバイスモード)で簡易的な画面サイズ切り替えを試す

---

## 11. CSSのFLOCSS形式へのリファクタリング
**質問:** CSSコードをFLOCSS形式に再編成してください
**詳細:**  
1. **CSSの分類:**
   - 現在のCSSコードを「Foundation」「Layout」「Object」「Component」などのフォルダに分類し、それぞれに対応するCSSファイルを作成する
   - 例:  
     ```
     src/assets/css/
     ├─ foundation/
     ├─ layout/
     ├─ object/
     └─ component/
     ```
   - 必要に応じて`@import`や`@use`を使って読み込む
2. **命名規則の統一:**
   - BEMやFLOCSSの命名規則に従い、一貫したクラス名に統一する  
   - HTML側のクラス指定も更新する
3. **動作確認:**
   - リファクタリング後、デザインが正しく適用されるかテストする  
   - コンソール上で404エラー(CSSファイルのパス不備)が出ないか確認する

**目的:**  
- 保守性と再利用性の高いCSS設計(FLOCSS)の概要を理解する

**注意点:**  
- HTMLやVueコンポーネントのクラス名、ファイルパスとの整合性を保つこと

**ヒント:**  
- FLOCSSとBEMのサンプルプロジェクトや公式ドキュメントを参考にする

---

## 12. Pinia導入のための状態管理設計
**質問:** Piniaを利用した基本的な状態管理設計を実装してください
**詳細:**  
1. **ストアの作成:**
   - 例として、`src/stores`ディレクトリを作成し、`useTaskStore.js`(または`.ts`)というファイルを作成する
   - `defineStore`をインポートし、タスク管理用のストアを定義する
   - ストアには、以下を含める:
     - `state`: タスクの配列などの初期状態
     - `getters`: タスク数などの派生的な値を返す
     - `actions`: タスクの追加・削除・完了切り替えなどの操作を実装
2. **ストアの利用:**
   - `main.js`(または`main.ts`)でPiniaを作成し、Vueアプリに登録する
     ```js
     import { createPinia } from 'pinia'
     const pinia = createPinia()
     createApp(App).use(pinia).mount('#app')
     ```
   - 任意のコンポーネント(例: `App.vue`)で`useTaskStore`をインポートし、ストアを利用する
     ```js
     import { useTaskStore } from '@/stores/useTaskStore'
     const taskStore = useTaskStore()
     ```

**目的:**  
- Piniaの基本概念(ストア、状態、ゲッター、アクション)を理解し、状態管理基盤を構築する

**注意点:**  
- ストアの命名規則やフォルダ構成を分かりやすく保つ

**ヒント:**  
- Piniaの公式ドキュメントを参照する

---

## 13. Piniaアクション・ゲッターの作成
**質問:** Piniaのアクションとゲッターを実装してください
**詳細:**  
1. **アクションの定義:**
   - `useTaskStore`内に、タスクの追加、削除、完了状態の切り替えに対応するアクションを定義する
   - 例:
     ```js
     addTask(newTask) { ... },
     deleteTask(id) { ... },
     toggleTask(id) { ... }
     ```
   - アクションはストアの`state`を直接更新する
2. **ゲッターの定義:**
   - `totalTasks`や`incompleteTasks`など、タスク情報を派生的に取得できるゲッターを定義する
   - ゲッターは計算結果だけを返し、`state`の読み取り専用ロジックを担う

**目的:**  
- Piniaの`state`/`getters`/`actions`の役割分担を理解し、複数のタスク操作が状態にどのように反映されるかを学ぶ

**注意点:**  
- ストアの状態はアクション経由で更新し、ゲッターは「読み取り専用」の概念であることを意識する

**ヒント:**  
- Piniaの公式ドキュメントの「State」「Getters」「Actions」を再確認する

---

## 14. Piniaを利用したタスク追加機能への改修
**質問:** Piniaを利用してタスク追加機能を改修してください
**詳細:**  
1. **ストアのアクション呼び出し:**
   - これまでコンポーネント内で直接操作していたタスク追加処理を、ストアの`addTask`アクションで行うように書き換える
   - 例:  
     ```js
     const taskStore = useTaskStore()
     function addTask() {
       taskStore.addTask(newTask.value)
       newTask.value = ''
     }
     ```
2. **UIの更新:**
   - ストアの状態に対して`v-for`でタスクを描画している部分があれば、ストアの`tasks`を参照するようにする
   - タスクが追加されると、ストアの状態変更に応じて UI が自動的に更新されることを確認する

**目的:**  
- Piniaを活用し、コンポーネントとストアの責務分担を明確にする

**注意点:**  
- コンポーネントからはストアのアクションだけを呼び出し、直接`state`を書き換えないようにする

**ヒント:**  
- `useTaskStore`を複数コンポーネントで呼び出すと、同じストアインスタンスを共有する仕組みを確認する

---

## 15. Piniaを利用したタスク削除・完了切替機能への改修
**質問:** Piniaを利用してタスク削除と完了切替機能を改修してください
**詳細:**  
1. **ストアのアクション呼び出し:**
   - 既存のコンポーネント(例: `App.vue`)内で`deleteTask`と`toggleTask`メソッドを呼び出していた部分を、ストアのアクション経由に書き換える
   - 例:
     ```html
     <button @click="taskStore.deleteTask(task.id)">削除</button>
     <button @click="taskStore.toggleTask(task.id)">
       {{ task.completed ? '未完了' : '完了' }}
     </button>
     ```
2. **UIの更新:**
   - ストアの状態が更新されると、リアクティブにタスク一覧が再レンダリングされることを確認する

**目的:**  
- Piniaを使って、複数のタスク操作（追加・削除・完了切替）を統一的に管理する仕組みを理解する

**注意点:**  
- コンポーネントから直接`tasks`配列を操作せず、必ずアクションを経由して変更する

**ヒント:**  
- `id`をキーに指定して、該当するタスクだけを変更・削除するロジックが正しく動くかテストする

---

## 16. JavaScript版アプリの総合動作確認と改善
**質問:** 全機能の統合動作確認と改善処理を実装してください
**詳細:**  
1. **統合テストの実施:**
   - タスク追加、削除、完了切替、バリデーション、ローカルストレージ永続化が連動して動作するか、シナリオベースでテストする
   - 例:  
     - 空入力→エラー表示→正常入力→登録→完了切替→ローカルストレージ保存→再読み込み…など
2. **デバッグとバグ修正:**
   - 開発者ツールやVue Devtoolsを用いて、エラーや警告を確認する
   - バグがあれば原因を特定し、修正する
3. **コードのリファクタリング:**
   - 重複コードの削除、関数分割、変数名の統一などを行い、全体の可読性と保守性を向上させる
4. **最終的な動作確認:**
   - ユーザー操作をシミュレートし、すべての機能が正常に動作することを確認する

**目的:**  
- 全機能の統合テストとデバッグ、コード改善を通じて、実務に近い開発プロセスを体験する

**注意点:**  
- エッジケース(空文字、重複タスク、極端に長い文字列など)も含めてテストする

**ヒント:**  
- Vue Devtoolsでストアの状態変化をリアルタイムに観察しながらデバッグできる

---

## 17. Vueコンポーネントの作成(TaskAdd)
**質問:** タスク追加用コンポーネント(TaskAdd)を作成してください
**詳細:**  
1. **コンポーネントの設計:**
   - `src/components`ディレクトリに`TaskAdd.vue`ファイルを作成し、タスク追加フォームのUIを実装する
   - 単一ファイルコンポーネント(SFC)形式で`<template>`, `<script>`, `<style>`を記述する
2. **フォームとイベント処理:**
   - `<template>`内にテキスト入力欄と追加ボタンを用意し、`v-model`で入力値を管理する
   - 追加ボタンをクリックしたら`emit('add-task', inputValue)`のように親コンポーネントへタスクデータを送る(`defineEmits`を使用)
3. **UIのリセットと状態管理:**
   - タスク送信後、入力欄をクリアして再度フォーカスを設定するなどの振る舞いを実装する
   - 必要に応じてバリデーションやエラー表示をこのコンポーネント内で行う

**目的:**  
- Vueの単一ファイルコンポーネントの作成と、`props`/`emit`を利用した親子コンポーネント間通信の基本を学ぶ

**注意点:**  
- `emit`イベント名やプロパティ名が衝突しないように注意する

**ヒント:**  
- Vue公式ドキュメントの「コンポーネント間通信」(PropsとEvents)を参照する

---

## 18. Vueコンポーネントの作成(TaskList)
**質問:** タスク一覧表示用コンポーネント(TaskList)を作成してください
**詳細:**  
1. **コンポーネントの設計:**
   - `src/components`ディレクトリに`TaskList.vue`ファイルを作成し、親(`App.vue`など)から渡されたタスク配列を`props`として受け取る
   - `<script setup>`なら`defineProps`、オブジェクト形式なら`props: { tasks: ... }`のように定義する
2. **リスト表示の実装:**
   - `<template>`内で`v-for="task in tasks"`を使い、各タスクを`<li>`として動的生成する
   - 一意な`key`属性を設定する(例：`:key="task.id"`)
3. **UIの連携と更新:**
   - 親コンポーネントがタスクを追加 / 削除 / 更新するたびに、`tasks`が変化し、`TaskList`が再レンダリングされることを確認する

**目的:**  
- `props`を用いたデータ受け渡しと、動的なリスト生成の基本を学ぶ

**注意点:**  
- `props`は読み取り専用であるため、リスト自体の更新は親またはストア側で行う

**ヒント:**  
- 「リストレンダリング」「propsとemit」の仕組みを再度復習する

---

## 19. Vueコンポーネントの作成(TaskInfo)
**質問:** タスク統計情報表示用コンポーネント(TaskInfo)を作成してください
**詳細:**  
1. **コンポーネントの設計:**  
   - `src/components/TaskInfo.vue`ファイルを作成し、親から統計情報(総タスク数、未完了タスク数)を`props`として受け取る  
   - データの加工は親側ですべて行い、表示専用のコンポーネントとして設計する
2. **UIの実装:**  
   - `<template>`内で統計情報を表示するためのシンプルな要素(例：`<div>`)を配置し、CSSでデザインを整える  
   - 例：
     ```html
     <template>
       <div class="task-info">
         <p>総タスク数: {{ total }}</p>
         <p>未完了タスク数: {{ incomplete }}</p>
       </div>
     </template>
     ```
   - `props`で受け取ったデータをそのまま表示するイメージ
3. **リアルタイム更新の確認:**  
   - 親コンポーネント側でタスク追加や削除が行われた際に、`props`として渡される統計情報が更新され、`TaskInfo`の表示が自動的に更新されることを確認する

**目的:**  
- データ表示専用コンポーネントの実装方法と、`props`を使った親→子データ受け渡しを理解する

**注意点:**  
- 親からのデータが正確に反映されるか必ずテストする(`undefined`や型違いがないかなど)

**ヒント:**  
- 単純表示専用コンポーネントでは「受け取ったらすぐ表示する」程度のシンプル実装で十分

---

## 20. コンポーネント間のデータ連携とイベントハンドリング
**質問:** 親子間でデータとイベントを連携させる処理を実装してください
**詳細:**  
1. **親から子へのデータ渡し:**
   - 親コンポーネント(例：`App.vue`など)で管理しているタスクリストや統計情報を、`props`として `TaskAdd`, `TaskList`, `TaskInfo`に渡す
   - 例：
     ```html
     <TaskAdd :initialTask="..." />
     <TaskList :tasks="tasks" />
     <TaskInfo :total="totalTasks" :incomplete="incompleteTasks" />
     ```
2. **子から親へのイベント通知:**
   - 子コンポーネント(例：`TaskAdd`, `TaskList`)でタスク追加・削除・完了切替などの操作が行われたら、`emit`を通じて親コンポーネントに通知する
   - 親コンポーネントは受け取ったイベントをハンドリングしてストアやローカルの状態を更新する
3. **状態更新と再レンダリング:**
   - 親コンポーネントが状態を更新し、その結果を再度`props`として子へ渡す仕組みを確認する(単方向データフロー)
   - 正しく連携していれば、UIが自動的に再レンダリングされる

**目的:**  
- 単方向データフロー(`props` ↓, `emit` ↑)とイベント連携の基本パターンを理解する

**注意点:**  
- `emit`と`props`の受け渡しタイミングに注意し、無限ループなどが発生しないかチェックする

**ヒント:**  
- Vue公式ドキュメントの「コンポーネント間通信」の項目を復習する

---

## 21. 主要コンポーネントのTypeScript変換(TaskAdd)
**質問:** TaskAddコンポーネントをTypeScriptに変換してください
**詳細:**  
1. **TaskAddコンポーネントの`<script lang="ts">`化:**
   - `.vue`ファイル内の`<script>`を`<script lang="ts">`に書き換える
   - `defineEmits`, `defineProps`などComposition APIを使う場合は、TypeScriptの文法規則を用いる
2. **適切な型定義の追加:**
   - 受け取る`props`や内部で利用する`ref`変数、`emit`のイベント名・ペイロードなどに対して、型定義(インターフェイスや型エイリアス)を追加する
   - 例：
     ```ts
     interface AddTaskProps {
       placeholder?: string
       maxLength?: number
     }

     const props = defineProps<AddTaskProps>()
     const emit = defineEmits<{
       (e: 'add-task', newText: string): void
     }>()
     ```

**目的:**  
- TypeScriptによる型安全な開発を実感し、コンポーネントの入力やイベントに型情報を付与するメリットを体験する

**注意点:**  
- 不足している型や誤った型を定義するとコンパイルエラーが出るので、エディタ等で確認しながら進める

**ヒント:**  
- Vue公式ドキュメントのTypeScriptガイドや、Volar/TS + Vueのサンプルを参考にする

---

## 22. 他のコンポーネントのTypeScript変換(TaskList、TaskInfo)
**質問:** TaskListおよびTaskInfoコンポーネントをTypeScriptに変換してください
**詳細:**  
1. **TaskList, TaskInfoの`<script lang="ts">`化:**
   - それぞれの`.vue`ファイルの`<script>`を`<script lang="ts">`に変更し、TypeScriptを利用できるようにする
2. **propsの型定義, データ構造の明確化:**
   - 親から受け取る`tasks`や`total`, `incomplete`などについて、型定義を行う
   - 例：
     ```ts
     interface Task {
       id: number
       text: string
       completed: boolean
     }

     interface TaskListProps {
       tasks: Task[]
     }
     ```
   - こうしたインターフェイスや型エイリアスを定義し、`defineProps<TaskListProps>()`のように適用する

**目的:**  
- 複数コンポーネントでTypeScriptの型定義を実践し、コードの可読性・保守性を高める

**注意点:**  
- コンポーネント間で共有する型は重複しないようにまとめる(`@/types`ディレクトリなどを作ってもよい)

**ヒント:**  
- 共通化できる型は専用ファイルにまとめてインポートする

---

## 23. Pinia関連ファイルのTypeScript対応
**質問:** Piniaの関連ファイルを TypeScript 対応に変更してください
**詳細:**  
1. **ストアファイルを`.ts`に変換:**
   - 例：`useTaskStore.js` → `useTaskStore.ts`に拡張子を変更し、`import type ...`などを利用して型定義を導入する
   - `defineStore`の戻り値やアクション、ゲッターの型を正確に定義する
2. **状態やゲッター、アクションに適切な型を付与:**
   - 例：
     ```ts
     interface Task {
       id: number
       text: string
       completed: boolean
     }

     export const useTaskStore = defineStore('task', {
       state: () => ({
         tasks: [] as Task[]
       }),
       getters: {
         totalTasks: (state) => state.tasks.length,
         incompleteTasks: (state) => state.tasks.filter(t => !t.completed).length
       },
       actions: {
         addTask(text: string) { ... }
         ...
       }
     })
     ```
3. **ストアを使用するコンポーネント側での型利用:**
   - `useTaskStore`をインポートした際に、推論された型がエディタで認識されるか確認する
   - 必要に応じてコンポーネント側でもインターフェイスをインポートし、propsや変数に型指定をする

**目的:**  
- PiniaでもTypeScriptをフル活用し、アプリ全体の型安全性を高める

**注意点:**  
- ファイル拡張子やインポートパスの変更漏れによるコンパイルエラーに注意

**ヒント:**  
- Pinia公式ドキュメントの「TypeScript」セクションや実装例を参照し、定義パターンを学ぶ

---

## 24. UI/UX改善のためのバリデーション強化
**質問:** 入力検証と視覚的フィードバックの強化処理を実装してください
**詳細:**  
1. **入力バリデーションの強化:**
   - 例：TaskAddコンポーネント内で、50文字以上の入力を禁止し、重複タスクも許可しないようにする
   - エラー条件時に追加ボタンを無効化(`disabled`)したり、エラーを表示するなどのロジックを追加する
2. **視覚的フィードバックの追加:**
   - エラー発生時に入力欄へ赤枠を表示する、エラー文をフェードインするなど、ユーザーが気付きやすい演出を行う
   - CSSの条件付きクラスやアニメーション(`transition`/`@keyframes`)を活用する

**目的:**  
- ユーザー体験を向上させるため、堅牢な入力検証とわかりやすいエラー表示を実践する

**注意点:**  
- 正常状態に戻った際にエラースタイルやメッセージが正しく消えるか検証する

**ヒント:**  
- Vueの「条件付きクラスバインディング」や「トランジション」を参照すると便利

---

## 25. コードとドキュメントの整備
**質問:** コードに十分なコメントとドキュメントを整備してください
**詳細:**  
1. **コード内コメントの充実:**
   - 各コンポーネントや関数、重要な処理ブロックに対して、JSDoc形式またはシンプルなコメントを追加する
   - 例：
     ```js
     /**
      * タスクを追加する
      * @param {string} text - 追加するタスクのテキスト
      */
     function addTask(text) { ... }
     ```
2. **README.mdの整備:**
   - プロジェクト概要、ディレクトリ構成、使用技術、学習ポイントなどを詳細にまとめる
   - 「起動方法」「依存関係のインストール」「ビルド方法」なども含めるとよい
3. **ドキュメント管理ツールの検討:**
   - 必要に応じてWikiやStorybook、Docusaurusなどのツールで設計や機能を可視化する

**目的:**  
- コードの可読性・保守性向上と、プロジェクト全体の設計意図を明確化するためのドキュメント整備を学ぶ

**注意点:**  
- 実装内容とドキュメントが齟齬しないよう、変更点があればREADMEやコメントも更新する

**ヒント:**  
- GitHub上のオープンソースや企業のリポジトリのREADMEを参考に、ドキュメントの書き方を学ぶ

---

## 26. 最終レビューとリファクタリング
**質問:** 全体の動作確認とコードレビュー、リファクタリング処理を実装してください
**詳細:**  
1. **全体機能の統合テスト:**
   - タスク追加、削除、完了切替、バリデーション、ローカルストレージ永続化など、すべての機能が連動して動作するかシナリオベースでテストする
2. **コードレビューの実施:**
   - チームやペアでコードレビューし、命名規則、コードの複雑度、可読性などの観点でフィードバックをもらう
   - 重要な指摘点はIssue化して管理するなどの運用を試す
3. **リファクタリングの実施:**
   - 指摘や改善点に基づき、コードの最適化・整理を行う（重複コードの削除、変数・関数名の統一、適切なファイル分割など）
4. **静的解析とフォーマッティング:**
   - VSCodeのフォーマッター などを使ってコードスタイルを整備する
   - 可能ならContinuous Integration(CI)パイプラインで自動チェックを導入してもよい

**目的:**  
- コードレビューとリファクタリングを通じて、実務さながらの品質向上プロセスを体験する

**注意点:**  
- リファクタリングによる不具合が混入しないよう、小さな単位でテストとコミットを繰り返す

**ヒント:**  
- Vue Devtoolsやブラウザのコンソール、CIツールなどを活用しながら段階的に修正する

---
